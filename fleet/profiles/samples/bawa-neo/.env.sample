# Bawa Neo Profile Environment
# Copy to ~/.hermes/profiles/bawa-neo/.env

# Neo's Telegram bot token
# TELEGRAM_BOT_TOKEN=your-neo-bot-token

# Gateway port (if running as persistent gateway)
# API_SERVER_PORT=8642
